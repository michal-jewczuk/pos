plugins {
    id 'java'
    id 'java-gradle-plugin'
    id 'maven'
}

version '1.0'

dependencies {
    compileOnly gradleApi()
}

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: mavenLocal().url)
        }
    }
}
